const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); (async () => { const org = await prisma.organization.findFirst({ where: { name: 'Aarogya Multispeciality Clinic' } }); const users = await prisma.user.count({ where: { organizationId: org.id }}); const patients = await prisma.patient.count({ where: { organizationId: org.id }}); const apts = await prisma.healthAppointment.count({ where: { organizationId: org.id }}); console.log('Org:', org.name, '\nUsers:', users, '\nPatients:', patients, '\nAppointments:', apts); await prisma.$disconnect(); })()
