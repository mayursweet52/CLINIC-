const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); (async () => { const sunrise = await prisma.organization.findFirst({ where: { name: 'Sunrise Clinic' } }); const users = await prisma.user.count({ where: { organizationId: sunrise.id }}); const patients = await prisma.patient.count({ where: { organizationId: sunrise.id }}); const apts = await prisma.healthAppointment.count({ where: { organizationId: sunrise.id }}); console.log('Sunrise Users:', users, 'Patients:', patients, 'Appointments:', apts); await prisma.$disconnect(); })()
