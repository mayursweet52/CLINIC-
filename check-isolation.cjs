const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); await page.goto('http://localhost:3000/login'); await page.fill('input[type="email"]', 'owner@sunrise.com'); await page.fill('input[type="password"]', 'Sunrise@2024'); await page.click('button[type="submit"]'); await page.waitForURL('**/admin'); await page.waitForTimeout(2000); await page.screenshot({ path: 'sunrise-admin.png' }); await page.goto('http://localhost:3000/admin/staff'); await page.waitForTimeout(2000); await page.screenshot({ path: 'sunrise-staff.png' }); console.log('Screenshots taken. Check sunrise-admin.png and sunrise-staff.png.'); await browser.close(); })()
