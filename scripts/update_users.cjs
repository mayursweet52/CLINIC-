const { PrismaClient } = require('@prisma/client'); const p = new PrismaClient(); async function m() { await p.user.update({ where: { id: 'doc-1' }, data: { email: 'dr.smith@clinic.com', name: 'Dr. Rajesh Sharma', department: 'Cardiology' } }); await p.user.update({ where: { id: 'admin-1' }, data: { email: 'admin@clinic.com', name: 'Admin' } }); console.log('Updated users doc-1 and admin-1!'); } m().catch(console.error).finally(()=>p.$disconnect());
