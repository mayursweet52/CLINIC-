const { chromium } = require('playwright'); const CREDS = { email: 'suresh.patel@aarogyaclinic.in', password: 'Pharma@2024' }; (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); await page.goto('http://localhost:3000/login'); await page.fill('input[type="email"]', CREDS.email); await page.fill('input[type="password"]', CREDS.password); await page.click('button[type="submit"]'); await page.waitForURL('**/pharmacy'); await page.waitForTimeout(2000); console.log('CONTENT MATCH:', await page.locator('button:has-text("Dispense")').count()); await browser.close(); })()
